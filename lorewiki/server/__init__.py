"""User-facing servers: FastAPI REST and MCP stdio. Phases 4-5.

The Streamlit UI was removed in 0.1.0; users now consume the knowledge
base via the CLI, the REST API, the MCP server, or by opening the
active topic's vault directory in any Markdown editor.
"""
